# Dependencias de los bots de descarga (Python).
# Instalar:  pip install -r requirements.txt
#            python -m playwright install chromium
playwright>=1.40
pywin32>=306

# Lector de planillas (lector/planilla.py).
openpyxl>=3.1
