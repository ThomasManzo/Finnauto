# Dependencias de finauto (Python).
# Instalar:  pip install -r requirements.txt
#            python -m playwright install chromium

# Automatizacion del navegador para los bots de descarga.
playwright>=1.40

# Credenciales en el llavero nativo del sistema operativo:
# macOS Keychain / Administrador de credenciales de Windows / Secret Service.
keyring>=24.0

# Lector de planillas (lector/planilla.py).
openpyxl>=3.1

# PDFs (informe/, clientes/<c>/propuesta.py).
reportlab>=4.0

# Lectores de extractos (lector/extractos.py): Excel viejos del home banking y PDFs.
xlrd>=2.0
pymupdf>=1.24
pypdf>=4.0
